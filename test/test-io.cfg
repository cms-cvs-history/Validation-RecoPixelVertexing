process PROD2 = {

  source = PoolSource {
    untracked vstring fileNames = {'file:pixel_vertexing.root'}

#     untracked vstring fileNames = {
#    '/store/unmerged/RelVal/2006/8/13/RelVal09010Muons1-10/GEN-SIM-DIGI-RECO/0005/EC233E68-1E2B-DB11-8406-0002B3D8E6A4.root'}

    untracked int32 maxEvents = -1
    untracked uint32 debugVerbosity = 0
    untracked bool   debugFlag     = true
  }

  service = MessageLogger { 
    untracked vstring destinations   = { "debug-io.txt" }
    untracked PSet debug-io.txt = { untracked string threshold = "DEBUG"} 
    untracked vstring debugModules = { "*" }
  }

  module trackVal = PixelTrackVal {
    string TrackCollection = "pixelTracks"
    untracked string  HistoFile = "pixelTrackHistos_ioVal.root"
    untracked uint32 Verbosity = 1
  }

  module vertexVal = PixelVertexTest {
    string TrackCollection = "pixelTracks"
    untracked string OutputTree = "pixelVertexHistos_ioVal.root"
    untracked uint32 Verbosity = 1
  }

  path p = {trackVal, vertexVal}

}

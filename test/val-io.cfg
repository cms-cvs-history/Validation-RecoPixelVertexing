process VALIO = {

  source = PoolSource {
    untracked vstring fileNames = {"file:pixel_vertexing.root"}
    untracked int32 maxEvents = 10
    untracked uint32 debugVerbosity = 0
    untracked bool   debugFlag     = true
  }

  module val = PixelVertexVal {
    untracked string OutputFile = "val-io.out"
    untracked uint32 Verbosity = 1
  }

  path p = {val}

}

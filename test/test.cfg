process PROD = {

  source = PoolSource {
#    untracked vstring fileNames = {'file:/afs/cern.ch/cms/Tutorials/july06/VertexReconstruction/sim_rec_10muons_1-10GeV_500evts.root'}

     untracked vstring fileNames = {
    '/store/unmerged/RelVal/2006/8/13/RelVal09010Muons1-10/GEN-SIM-DIGI-RECO/0005/EC233E68-1E2B-DB11-8406-0002B3D8E6A4.root'}

    untracked int32 maxEvents = -1
    untracked uint32 debugVerbosity = 0
    untracked bool   debugFlag     = true
  }

  include "RecoPixelVertexing/PixelTrackFitting/data/PixelTracks.cff"  
  include "RecoPixelVertexing/PixelVertexFinding/data/PixelVertexes.cff"  

  module test = PixelVertexTest {
    string TrackCollection = "pixelTracks"
    untracked string OutputTree = "pixel_vertices.root"
    untracked uint32 Verbosity = 1
  }

  module out = PoolOutputModule 
  {
    untracked string fileName = "pixel_vertexing.root"
    // uncomment if reco data must be stored
#    untracked vstring outputCommands =
#    {
#       "drop *"
#    }
  }

  path p = {pixelTracks, pixelVertices, test}

  endpath e = 
  {
    out
  }
}

